{
  "name": "zapflow-backend",
  "version": "1.0.0",
  "description": "Backend API para ZapFlow com PostgreSQL",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "echo 'Backend não requer build - Node.js puro' && exit 0",
    "migrate": "node scripts/migrate.js",
    "create-admin": "node scripts/create-admin.js",
    "update-user-name": "node scripts/update-user-name.js",
    "validate-users": "node scripts/validate-users.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}

